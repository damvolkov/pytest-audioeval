{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pytest-audioeval","text":"<p>Pytest plugin for STT/TTS integration testing with httpx, metrics, and embedded audio samples.</p> <p>pytest-audioeval gives you a single <code>audioeval</code> fixture that exposes STT and TTS clients over four transport protocols (HTTP POST, chunked streaming, SSE, WebSocket), built-in quality metrics (WER, CER, PESQ MOS), and a catalog of ground-truth audio samples \u2014 all wired through pytest CLI options.</p>"},{"location":"#at-a-glance","title":"At a Glance","text":"<pre><code>async def test_stt_transcription(audioeval):\n    sample = audioeval.samples.en_hello_world\n\n    async with audioeval.stt.ws(sample=sample) as session:\n        await session.send_sample(sample)\n        transcript = await session.receive_text()\n\n    result = session.result()\n    result.assert_quality(max_wer=0.2)\n</code></pre> <pre><code>async def test_tts_synthesis(audioeval):\n    response = await audioeval.tts.post(json={\n        \"input\": \"Hello world.\",\n        \"model\": \"kokoro\",\n        \"voice\": \"af_heart\",\n        \"response_format\": \"wav\",\n    })\n    assert response.status_code == 200\n    assert len(response.content) &gt; 0\n</code></pre>"},{"location":"#features","title":"Features","text":"Feature Description 4 transport protocols HTTP POST, chunked streaming, SSE, WebSocket \u2014 for both STT and TTS Text metrics WER and CER via jiwer with chainable assertions Audio metrics PESQ MOS via pesq for perceptual quality scoring Embedded samples Ground-truth WAV + transcription pairs, auto-discovered from <code>samples/audio/</code> CLI-driven thresholds <code>--audioeval-wer</code>, <code>--audioeval-cer</code>, <code>--audioeval-mos</code> configure quality gates Pure httpx Built on httpx, httpx-ws, and httpx-sse Async-native All I/O is <code>async/await</code>, integrates with <code>pytest-asyncio</code> Typed Full PEP 561 type stubs, <code>py.typed</code> marker included"},{"location":"#protocol-matrix","title":"Protocol Matrix","text":"<p>Both <code>STTClient</code> and <code>TTSClient</code> expose the same four transport methods:</p> Method Protocol Use case <code>.post()</code> HTTP POST Batch request/response (e.g. Whisper API, Kokoro) <code>.stream()</code> Chunked HTTP Streaming responses via <code>aiter_bytes()</code> / <code>aiter_lines()</code> <code>.sse()</code> Server-Sent Events SSE-compatible endpoints via <code>EventSource</code> <code>.ws()</code> WebSocket Full-duplex streaming (e.g. WhisperLive, real-time TTS)"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li> Installation \u2014 Install with pip or uv</li> <li> Quick Start \u2014 Write your first audio test in 5 minutes</li> <li> Configuration \u2014 CLI options, fixtures, thresholds</li> <li> Advanced Usage \u2014 Roundtrips, custom samples, metrics</li> <li> API Reference \u2014 Auto-generated from source</li> </ul>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#010","title":"0.1.0","text":"<p>Initial release.</p> <ul> <li>STTClient with 4 transport protocols: <code>post()</code>, <code>stream()</code>, <code>sse()</code>, <code>ws()</code></li> <li>TTSClient with 4 transport protocols: <code>post()</code>, <code>stream()</code>, <code>sse()</code>, <code>ws()</code></li> <li>STTSession with built-in latency tracking, fragment accumulation, and auto-metrics</li> <li>STTResult with chainable <code>compute_metrics()</code> and <code>assert_quality()</code></li> <li>TextMetrics \u2014 WER/CER via jiwer with detailed edit distance breakdown</li> <li>AudioMetrics \u2014 PESQ MOS via pesq for perceptual audio quality</li> <li>SampleRegistry \u2014 auto-discovery of ground-truth WAV + TXT pairs</li> <li>Embedded samples \u2014 3 English ground-truth recordings (hello_world, quick_brown_fox, counting)</li> <li><code>audioeval</code> fixture \u2014 session-scoped, CLI-configured facade</li> <li><code>audioeval_thresholds</code> fixture \u2014 per-test quality gates from CLI options</li> <li>CLI options: <code>--stt-url</code>, <code>--tts-url</code>, <code>--audioeval-wer</code>, <code>--audioeval-cer</code>, <code>--audioeval-mos</code></li> <li>Full PEP 561 typing with <code>py.typed</code> marker</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python &gt;= 3.13</li> <li>A running STT and/or TTS service to test against</li> </ul>"},{"location":"getting-started/installation/#install-the-plugin","title":"Install the plugin","text":"uvpip <pre><code>uv add --group dev pytest-audioeval\n</code></pre> <pre><code>pip install pytest-audioeval\n</code></pre> <p>This pulls in the core dependencies:</p> Package Purpose <code>pytest &gt;= 8.0</code> Test framework <code>httpx &gt;= 0.27</code> Async HTTP client <code>httpx-ws &gt;= 0.8.2</code> WebSocket transport <code>httpx-sse &gt;= 0.4.3</code> Server-Sent Events transport <code>jiwer &gt;= 3.0</code> WER/CER computation <code>pesq &gt;= 0.0.4</code> PESQ MOS audio quality <code>numpy &gt;= 2.0</code> Audio array operations <code>soundfile &gt;= 0.12</code> WAV file I/O"},{"location":"getting-started/installation/#verify-installation","title":"Verify installation","text":"<p>After installing, verify the plugin is registered:</p> <pre><code>pytest --co -q\n</code></pre> <p>You should see the <code>audioeval</code> options in:</p> <pre><code>pytest --help | grep audioeval\n</code></pre> <pre><code>Audio evaluation options:\n  --stt-url=STT_URL     STT service WebSocket URL\n  --tts-url=TTS_URL     TTS service HTTP URL\n  --audioeval-wer=AUDIOEVAL_WER\n                        Max WER threshold\n  --audioeval-cer=AUDIOEVAL_CER\n                        Max CER threshold\n  --audioeval-mos=AUDIOEVAL_MOS\n                        Min PESQ MOS threshold\n</code></pre>"},{"location":"getting-started/installation/#recommended-dev-dependencies","title":"Recommended dev dependencies","text":"<p>For development with pytest-audioeval, add the async test runner:</p> uvpip <pre><code>uv add --group dev pytest-asyncio\n</code></pre> <pre><code>pip install pytest-asyncio\n</code></pre> <p>And configure pytest for async:</p> pyproject.toml<pre><code>[tool.pytest.ini_options]\nasyncio_mode = \"auto\"\n</code></pre>"},{"location":"getting-started/installation/#infrastructure-setup","title":"Infrastructure setup","text":"<p>pytest-audioeval tests against real STT/TTS services. A typical setup uses Docker Compose:</p> compose.integration.yml<pre><code>services:\n  whisper-live:\n    image: ghcr.io/collabora/whisperlive-gpu:latest\n    ports:\n      - \"45120:9090\"\n    deploy:\n      resources:\n        reservations:\n          devices:\n            - capabilities: [gpu]\n\n  kokoro-tts:\n    image: ghcr.io/remsky/kokoro-fastapi:latest\n    ports:\n      - \"45130:8880\"\n    deploy:\n      resources:\n        reservations:\n          devices:\n            - capabilities: [gpu]\n</code></pre> <pre><code>docker compose -f compose.integration.yml up -d\n</code></pre> <p>Then run your tests pointing at the services:</p> <pre><code>pytest tests/ \\\n  --stt-url=ws://localhost:45120 \\\n  --tts-url=http://localhost:45130/v1/audio/speech\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>This guide walks you through writing your first STT and TTS tests with pytest-audioeval.</p>"},{"location":"getting-started/quickstart/#minimal-pytest-configuration","title":"Minimal pytest configuration","text":"pyproject.toml<pre><code>[tool.pytest.ini_options]\nasyncio_mode = \"auto\"\n</code></pre> <p>All tests use the <code>audioeval</code> fixture, which is session-scoped and auto-configured from CLI options.</p>"},{"location":"getting-started/quickstart/#stt-websocket-streaming","title":"STT \u2014 WebSocket streaming","text":"<p>The most common STT pattern: stream audio over WebSocket and check the transcript.</p> tests/test_stt.py<pre><code>import asyncio\nimport uuid\n\nimport orjson as json\n\nfrom pytest_audioeval.client import AudioEval\nfrom pytest_audioeval.metrics.text import TextMetrics\n\n\nasync def test_stt_websocket(audioeval: AudioEval):\n    \"\"\"Stream audio over WebSocket, get transcript, check WER.\"\"\"\n    assert audioeval.stt is not None, \"--stt-url required\"\n    sample = audioeval.samples.en_hello_world\n\n    async with audioeval.stt.ws(sample=sample) as session:\n        # Send config (WhisperLive protocol)\n        config = json.dumps({\n            \"uid\": str(uuid.uuid4()),\n            \"language\": \"en\",\n            \"task\": \"transcribe\",\n            \"model\": \"large-v3-turbo\",\n            \"use_vad\": True,\n        }).decode()\n        await session.send_text(config)\n\n        # Wait for server ready\n        ready = await session.receive_text()\n        assert \"SERVER_READY\" in ready\n\n        # Stream audio in chunks with realistic pacing\n        await session.send_sample(sample, chunk_ms=200)\n        await asyncio.sleep(2)\n        await session.send_text(\"END_OF_AUDIO\")\n\n        # Collect transcript segments\n        segments = []\n        while True:\n            try:\n                raw = await session.receive_text(timeout=5.0)\n                msg = json.loads(raw)\n                if \"segments\" in msg:\n                    segments = msg[\"segments\"]\n            except Exception:\n                break\n\n    transcript = \" \".join(\n        seg.get(\"text\", \"\").strip()\n        for seg in segments\n        if seg.get(\"text\", \"\").strip()\n    )\n    assert len(transcript) &gt; 0\n\n    metrics = TextMetrics.compute(\n        sample.reference_text.lower(),\n        transcript.lower(),\n    )\n    assert metrics.wer &lt;= 0.3\n</code></pre> <p>Run it:</p> <pre><code>pytest tests/test_stt.py --stt-url=ws://localhost:45120\n</code></pre>"},{"location":"getting-started/quickstart/#stt-http-batch","title":"STT \u2014 HTTP batch","text":"<p>For STT services with an HTTP POST endpoint (e.g. OpenAI Whisper API):</p> tests/test_stt_batch.py<pre><code>async def test_stt_batch(audioeval: AudioEval):\n    \"\"\"Send audio via HTTP POST, get transcript back.\"\"\"\n    assert audioeval.stt is not None\n    sample = audioeval.samples.en_hello_world\n\n    response = await audioeval.stt.post(\n        data=sample.audio_bytes(),\n        headers={\"Content-Type\": \"audio/wav\"},\n    )\n    assert response.status_code == 200\n    transcript = response.json()[\"text\"]\n    assert len(transcript) &gt; 0\n</code></pre>"},{"location":"getting-started/quickstart/#tts-http-batch","title":"TTS \u2014 HTTP batch","text":"<p>Send text, get audio back as a single response:</p> tests/test_tts.py<pre><code>import io\nimport soundfile as sf\n\nfrom pytest_audioeval.client import AudioEval\n\n\nasync def test_tts_batch(audioeval: AudioEval):\n    \"\"\"POST text, get WAV audio back.\"\"\"\n    assert audioeval.tts is not None\n\n    response = await audioeval.tts.post(json={\n        \"input\": \"Hello world.\",\n        \"model\": \"kokoro\",\n        \"voice\": \"af_heart\",\n        \"response_format\": \"wav\",\n        \"stream\": False,\n    })\n    data, rate = sf.read(io.BytesIO(response.content), dtype=\"float32\")\n    assert rate == 24_000\n    assert len(data) &gt; 0\n</code></pre>"},{"location":"getting-started/quickstart/#tts-chunked-streaming","title":"TTS \u2014 Chunked streaming","text":"<p>For TTS services that stream audio in chunks:</p> tests/test_tts_streaming.py<pre><code>async def test_tts_streaming(audioeval: AudioEval):\n    \"\"\"Receive audio in chunks via HTTP streaming.\"\"\"\n    assert audioeval.tts is not None\n\n    chunks = []\n    async with audioeval.tts.stream(json={\n        \"input\": \"Streaming test.\",\n        \"model\": \"kokoro\",\n        \"voice\": \"af_heart\",\n        \"response_format\": \"wav\",\n        \"stream\": True,\n    }) as response:\n        async for chunk in response.aiter_bytes():\n            chunks.append(chunk)\n\n    assert len(chunks) &gt; 0\n</code></pre> <p>Run it:</p> <pre><code>pytest tests/test_tts.py --tts-url=http://localhost:45130/v1/audio/speech\n</code></pre>"},{"location":"getting-started/quickstart/#using-embedded-samples","title":"Using embedded samples","text":"<p>The plugin ships with ground-truth audio samples:</p> <pre><code>async def test_browse_samples(audioeval: AudioEval):\n    # Access by name\n    sample = audioeval.samples.en_hello_world\n    assert sample.reference_text == \"Hello world.\"\n\n    # Access audio data\n    audio = sample.audio_numpy()      # float32 numpy array\n    raw = sample.audio_bytes()         # raw WAV bytes\n    chunks = sample.chunks(chunk_ms=100)  # PCM chunks for streaming\n\n    # Browse catalog\n    all_samples = audioeval.samples.all()\n    en_samples = audioeval.samples.by_lang(SampleLang.EN)\n</code></pre>"},{"location":"getting-started/quickstart/#standalone-metrics","title":"Standalone metrics","text":"<p>You can use metrics directly, without the <code>audioeval</code> fixture:</p> <pre><code>from pytest_audioeval.metrics.text import TextMetrics\n\nasync def test_text_quality():\n    metrics = TextMetrics.compute(\n        reference=\"the quick brown fox\",\n        hypothesis=\"the quick brown dock\",\n    )\n    assert metrics.wer &lt; 0.3\n    assert metrics.cer &lt; 0.1\n    assert metrics.substitutions == 1\n</code></pre>"},{"location":"getting-started/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Configuration \u2014 CLI options, thresholds, fixtures</li> <li>Advanced Usage \u2014 Roundtrips, custom samples, SSE, WebSocket TTS</li> <li>API Reference \u2014 Full class and method documentation</li> </ul>"},{"location":"guides/advanced-usage/","title":"Advanced Usage","text":""},{"location":"guides/advanced-usage/#transport-protocols","title":"Transport Protocols","text":"<p>Both <code>STTClient</code> and <code>TTSClient</code> support four transport protocols. Pick the one that matches your service.</p>"},{"location":"guides/advanced-usage/#http-post-post","title":"HTTP POST \u2014 <code>.post()</code>","text":"<p>Single request/response. Best for batch processing.</p> STTTTS <pre><code>async def test_stt_post(audioeval: AudioEval):\n    sample = audioeval.samples.en_hello_world\n    response = await audioeval.stt.post(\n        data=sample.audio_bytes(),\n        headers={\"Content-Type\": \"audio/wav\"},\n    )\n    transcript = response.json()[\"text\"]\n</code></pre> <pre><code>async def test_tts_post(audioeval: AudioEval):\n    response = await audioeval.tts.post(json={\n        \"input\": \"Hello world.\",\n        \"model\": \"kokoro\",\n        \"voice\": \"af_heart\",\n        \"response_format\": \"wav\",\n    })\n    audio_bytes = response.content\n</code></pre>"},{"location":"guides/advanced-usage/#chunked-streaming-stream","title":"Chunked Streaming \u2014 <code>.stream()</code>","text":"<p>HTTP streaming for large responses. Yields <code>httpx.Response</code> for <code>aiter_bytes()</code> / <code>aiter_lines()</code>.</p> STTTTS <pre><code>async def test_stt_stream(audioeval: AudioEval):\n    sample = audioeval.samples.en_hello_world\n    async with audioeval.stt.stream(data=sample.audio_bytes()) as response:\n        async for chunk in response.aiter_lines():\n            partial = json.loads(chunk)\n            print(partial[\"text\"])\n</code></pre> <pre><code>async def test_tts_stream(audioeval: AudioEval):\n    chunks = []\n    async with audioeval.tts.stream(json={\n        \"input\": \"Streaming test.\",\n        \"model\": \"kokoro\",\n        \"stream\": True,\n    }) as response:\n        async for chunk in response.aiter_bytes():\n            chunks.append(chunk)\n</code></pre>"},{"location":"guides/advanced-usage/#server-sent-events-sse","title":"Server-Sent Events \u2014 <code>.sse()</code>","text":"<p>For SSE-compatible endpoints. Yields <code>httpx_sse.EventSource</code>.</p> STTTTS <pre><code>async def test_stt_sse(audioeval: AudioEval):\n    sample = audioeval.samples.en_hello_world\n    async with audioeval.stt.sse(data=sample.audio_bytes()) as event_source:\n        async for event in event_source.aiter_sse():\n            if event.event == \"transcript\":\n                print(event.data)\n</code></pre> <pre><code>async def test_tts_sse(audioeval: AudioEval):\n    async with audioeval.tts.sse(json={\n        \"input\": \"Hello via SSE.\",\n        \"model\": \"kokoro\",\n    }) as event_source:\n        async for event in event_source.aiter_sse():\n            if event.event == \"audio\":\n                audio_chunk = base64.b64decode(event.data)\n</code></pre> <p>Accept header</p> <p><code>.sse()</code> automatically sets <code>Accept: text/event-stream</code>. You don't need to add it manually.</p>"},{"location":"guides/advanced-usage/#websocket-ws","title":"WebSocket \u2014 <code>.ws()</code>","text":"<p>Full-duplex streaming. STT returns an <code>STTSession</code> with built-in metrics; TTS returns a raw <code>AsyncWebSocketSession</code>.</p> STTTTS <pre><code>async def test_stt_ws(audioeval: AudioEval):\n    sample = audioeval.samples.en_hello_world\n\n    async with audioeval.stt.ws(sample=sample) as session:\n        await session.send_sample(sample, chunk_ms=200)\n        transcript = await session.receive_text(timeout=5.0)\n\n    result = session.result()\n    result.assert_quality(max_wer=0.2)\n</code></pre> <pre><code>async def test_tts_ws(audioeval: AudioEval):\n    async with audioeval.tts.ws() as session:\n        await session.send_text(json.dumps({\n            \"text\": \"Hello via WebSocket.\",\n            \"voice\": \"af_heart\",\n        }))\n        audio = await session.receive_bytes()\n</code></pre>"},{"location":"guides/advanced-usage/#sttsession-built-in-metrics","title":"STTSession \u2014 Built-in Metrics","text":"<p>When using <code>stt.ws()</code>, you get an <code>STTSession</code> that tracks latency, accumulates fragments, and auto-computes metrics:</p> <pre><code>async with audioeval.stt.ws(sample=sample) as session:\n    await session.send_sample(sample)\n    await session.receive_text()\n    await session.receive_text()\n\nresult = session.result()\n# result.hypothesis_text  \u2014 joined fragments\n# result.latency_ms       \u2014 wall-clock time\n# result.chunks_received  \u2014 number of receive_text calls\n# result.fragments        \u2014 raw received texts\n# result.text_metrics     \u2014 auto-computed if sample provided\n</code></pre>"},{"location":"guides/advanced-usage/#chainable-assertions","title":"Chainable assertions","text":"<p>Both <code>compute_metrics()</code> and <code>assert_quality()</code> return <code>Self</code>:</p> <pre><code>result = STTResult(hypothesis_text=\"hello world\")\nresult.compute_metrics(\"hello world\").assert_quality(max_wer=0.2, max_cer=0.15)\n</code></pre>"},{"location":"guides/advanced-usage/#tts-stt-roundtrip","title":"TTS \u2192 STT Roundtrip","text":"<p>Test the full pipeline: synthesize audio, then transcribe it back and compare:</p> <pre><code>import io\nimport numpy as np\nimport soundfile as sf\n\nasync def test_roundtrip(audioeval: AudioEval):\n    assert audioeval.tts is not None\n    assert audioeval.stt is not None\n\n    sample = audioeval.samples.en_hello_world\n\n    # 1. TTS: generate audio\n    response = await audioeval.tts.post(json={\n        \"input\": sample.reference_text,\n        \"model\": \"kokoro\",\n        \"voice\": \"af_heart\",\n        \"response_format\": \"wav\",\n    })\n    tts_audio, tts_rate = sf.read(\n        io.BytesIO(response.content), dtype=\"float32\",\n    )\n\n    # 2. Resample if needed (e.g. 24kHz TTS \u2192 16kHz STT)\n    if tts_rate != 16_000:\n        duration = len(tts_audio) / tts_rate\n        new_len = int(duration * 16_000)\n        indices = np.linspace(0, len(tts_audio) - 1, new_len)\n        tts_audio = np.interp(\n            indices, np.arange(len(tts_audio)), tts_audio,\n        ).astype(np.float32)\n\n    # 3. STT: transcribe\n    async with audioeval.stt.ws() as session:\n        chunk_size = 4096 * 4\n        audio_bytes = tts_audio.tobytes()\n        for i in range(0, len(audio_bytes), chunk_size):\n            await session.send_bytes(audio_bytes[i:i + chunk_size])\n\n    # 4. Verify\n    from pytest_audioeval.metrics.text import TextMetrics\n    metrics = TextMetrics.compute(\n        sample.reference_text.lower(),\n        transcript.lower(),\n    )\n    assert metrics.wer &lt;= 0.5\n</code></pre>"},{"location":"guides/advanced-usage/#audio-quality-pesq-mos","title":"Audio Quality \u2014 PESQ MOS","text":"<p>Compare synthesized audio against reference using PESQ:</p> <pre><code>from pytest_audioeval.metrics.audio import AudioMetrics\n\nasync def test_audio_quality():\n    reference = np.random.randn(16_000).astype(np.float32)\n    hypothesis = reference + 0.01 * np.random.randn(16_000).astype(np.float32)\n\n    metrics = AudioMetrics.compute(\n        reference, hypothesis, sample_rate=16_000,\n    )\n    metrics.assert_quality(min_mos=3.0)\n    print(f\"PESQ MOS: {metrics.mos:.2f}\")\n</code></pre> <p>PESQ requirements</p> <p>PESQ operates on 16kHz or 8kHz audio. Both arrays must have the same length and sample rate.</p>"},{"location":"guides/advanced-usage/#custom-samples","title":"Custom Samples","text":"<p>Register your own ground-truth samples:</p> conftest.py<pre><code>from pathlib import Path\nimport pytest\nfrom pytest_audioeval.client import AudioEval\nfrom pytest_audioeval.samples.registry import AudioSample, SampleLang\n\n@pytest.fixture(scope=\"session\")\ndef audioeval_with_custom(audioeval: AudioEval) -&gt; AudioEval:\n    audioeval.samples.register(AudioSample(\n        name=\"medical_term\",\n        lang=SampleLang.EN,\n        reference_text=\"The patient presents with dyspnea.\",\n        audio_path=Path(\"tests/resources/medical_term.wav\"),\n        sample_rate=16_000,\n    ))\n    return audioeval\n</code></pre> <p>Then use in tests:</p> <pre><code>async def test_medical_stt(audioeval_with_custom):\n    sample = audioeval_with_custom.samples.en_medical_term\n    # ... test with domain-specific audio\n</code></pre>"},{"location":"guides/advanced-usage/#sample-directory-convention","title":"Sample directory convention","text":"<p>Place WAV + TXT pairs in the directory structure:</p> <pre><code>samples/audio/\n\u251c\u2500\u2500 en/\n\u2502   \u251c\u2500\u2500 hello_world.wav\n\u2502   \u251c\u2500\u2500 hello_world.txt    # \"Hello world.\"\n\u2502   \u251c\u2500\u2500 custom_phrase.wav\n\u2502   \u2514\u2500\u2500 custom_phrase.txt  # \"Your custom phrase.\"\n\u2514\u2500\u2500 es/\n    \u251c\u2500\u2500 hola_mundo.wav\n    \u2514\u2500\u2500 hola_mundo.txt     # \"Hola mundo.\"\n</code></pre> <p>The registry auto-discovers these on initialization. Access via <code>samples.en_hello_world</code>, <code>samples.es_hola_mundo</code>, etc.</p>"},{"location":"guides/advanced-usage/#embedded-samples","title":"Embedded Samples","text":"<p>The plugin ships with three English ground-truth samples:</p> Key Reference Text Duration <code>en_hello_world</code> \"Hello world.\" ~1.5s <code>en_quick_brown_fox</code> \"The quick brown fox jumps over the lazy dog.\" ~2.5s <code>en_counting</code> \"One two three four five six seven eight nine ten.\" ~2.0s <p>All recorded at 16kHz, mono, float32.</p> <pre><code>async def test_sample_access(audioeval: AudioEval):\n    sample = audioeval.samples.en_quick_brown_fox\n\n    sample.name            # \"quick_brown_fox\"\n    sample.lang            # SampleLang.EN\n    sample.reference_text  # \"The quick brown fox...\"\n    sample.sample_rate     # 16_000\n    sample.duration_ms     # ~2500\n\n    sample.audio_numpy()   # np.ndarray (float32)\n    sample.audio_bytes()   # raw WAV bytes\n    sample.chunks(200)     # list[bytes] \u2014 200ms PCM chunks\n</code></pre>"},{"location":"guides/advanced-usage/#filtering-samples","title":"Filtering samples","text":"<pre><code>from pytest_audioeval.samples.registry import SampleLang\n\n# All English samples\nen = audioeval.samples.by_lang(SampleLang.EN)\n\n# Total count\ntotal = len(audioeval.samples)\n\n# Membership check\nexists = \"en_hello_world\" in audioeval.samples\n</code></pre>"},{"location":"guides/configuration/","title":"Configuration","text":""},{"location":"guides/configuration/#cli-options","title":"CLI Options","text":"<p>All configuration is passed via pytest CLI options. No config files needed.</p> <pre><code>pytest tests/ \\\n  --stt-url=ws://localhost:45120 \\\n  --tts-url=http://localhost:45130/v1/audio/speech \\\n  --audioeval-wer=0.15 \\\n  --audioeval-cer=0.10 \\\n  --audioeval-mos=3.5\n</code></pre> Option Type Default Description <code>--stt-url</code> <code>str</code> <code>None</code> STT service URL (HTTP or WebSocket) <code>--tts-url</code> <code>str</code> <code>None</code> TTS service URL (HTTP or WebSocket) <code>--audioeval-wer</code> <code>float</code> <code>0.2</code> Maximum Word Error Rate threshold <code>--audioeval-cer</code> <code>float</code> <code>0.15</code> Maximum Character Error Rate threshold <code>--audioeval-mos</code> <code>float</code> <code>3.0</code> Minimum PESQ MOS threshold (1.0\u20135.0 scale) <p>Optional services</p> <p>Both <code>--stt-url</code> and <code>--tts-url</code> are optional. If omitted, <code>audioeval.stt</code> or <code>audioeval.tts</code> will be <code>None</code>. Guard your tests accordingly:</p> <pre><code>async def test_stt(audioeval: AudioEval):\n    assert audioeval.stt is not None, \"--stt-url required\"\n</code></pre>"},{"location":"guides/configuration/#fixtures","title":"Fixtures","text":"<p>The plugin registers two fixtures:</p>"},{"location":"guides/configuration/#audioeval","title":"<code>audioeval</code>","text":"<p>Scope: <code>session</code></p> <p>The main entry point. Provides access to STT/TTS clients and the sample registry.</p> <pre><code>async def test_example(audioeval: AudioEval):\n    audioeval.stt      # STTClient | None\n    audioeval.tts      # TTSClient | None\n    audioeval.samples  # SampleRegistry (always available)\n</code></pre> <p>The fixture creates clients based on CLI options and tears them down at the end of the session.</p>"},{"location":"guides/configuration/#audioeval_thresholds","title":"<code>audioeval_thresholds</code>","text":"<p>Scope: <code>function</code></p> <p>A dictionary of CLI-driven quality thresholds:</p> <pre><code>async def test_thresholds(audioeval_thresholds: dict[str, float]):\n    max_wer = audioeval_thresholds[\"max_wer\"]   # from --audioeval-wer\n    max_cer = audioeval_thresholds[\"max_cer\"]   # from --audioeval-cer\n    min_mos = audioeval_thresholds[\"min_mos\"]   # from --audioeval-mos\n</code></pre> <p>Use it to build configurable quality gates:</p> <pre><code>async def test_stt_quality(audioeval, audioeval_thresholds):\n    result = STTResult(hypothesis_text=\"hello world\")\n    result.compute_metrics(\"hello world\")\n    result.assert_quality(\n        max_wer=audioeval_thresholds[\"max_wer\"],\n        max_cer=audioeval_thresholds[\"max_cer\"],\n    )\n</code></pre>"},{"location":"guides/configuration/#pytest-configuration","title":"pytest configuration","text":""},{"location":"guides/configuration/#async-mode","title":"Async mode","text":"<p>pytest-audioeval is fully async. Configure pytest-asyncio:</p> pyproject.toml<pre><code>[tool.pytest.ini_options]\nasyncio_mode = \"auto\"\nasyncio_default_fixture_loop_scope = \"session\"\n</code></pre> <p>Setting <code>asyncio_default_fixture_loop_scope = \"session\"</code> is required for the session-scoped <code>audioeval</code> fixture to share the event loop with your tests.</p>"},{"location":"guides/configuration/#import-mode","title":"Import mode","text":"<p>If your test directory has files with the same name across subdirectories (e.g. <code>tests/unit/test_stt.py</code> and <code>tests/integration/test_stt.py</code>), use importlib mode:</p> pyproject.toml<pre><code>[tool.pytest.ini_options]\naddopts = [\"--import-mode=importlib\"]\n</code></pre>"},{"location":"guides/configuration/#environment-patterns","title":"Environment patterns","text":""},{"location":"guides/configuration/#cicd","title":"CI/CD","text":"<p>In CI, pass URLs as environment variables and forward them to pytest:</p> .github/workflows/test.yml<pre><code>jobs:\n  integration:\n    services:\n      whisper:\n        image: ghcr.io/collabora/whisperlive-gpu:latest\n        ports: [\"45120:9090\"]\n      kokoro:\n        image: ghcr.io/remsky/kokoro-fastapi:latest\n        ports: [\"45130:8880\"]\n    steps:\n      - run: |\n          pytest tests/integration/ \\\n            --stt-url=ws://localhost:45120 \\\n            --tts-url=http://localhost:45130/v1/audio/speech\n</code></pre>"},{"location":"guides/configuration/#conditional-tests","title":"Conditional tests","text":"<p>Skip tests when a service is unavailable:</p> <pre><code>async def test_stt_only_when_available(audioeval: AudioEval):\n    if audioeval.stt is None:\n        pytest.skip(\"STT service not configured\")\n    # ... test logic\n</code></pre>"},{"location":"guides/configuration/#makefile-targets","title":"Makefile targets","text":"<pre><code>test-integration:\n    @pytest tests/integration/ \\\n        --stt-url=ws://localhost:45120 \\\n        --tts-url=http://localhost:45130/v1/audio/speech\n</code></pre>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>pytest_audioeval<ul> <li>client</li> <li>metrics<ul> <li>audio</li> <li>text</li> </ul> </li> <li>plugin</li> <li>samples<ul> <li>registry</li> </ul> </li> <li>stt</li> <li>tts</li> </ul> </li> </ul>"},{"location":"reference/pytest_audioeval/client/","title":"client","text":""},{"location":"reference/pytest_audioeval/client/#pytest_audioeval.client","title":"client","text":"<p>AudioEval facade \u2014 main fixture interface.</p>"},{"location":"reference/pytest_audioeval/client/#pytest_audioeval.client.AudioEval","title":"AudioEval","text":"<pre><code>AudioEval(\n    *,\n    stt_url: str | None = None,\n    tts_url: str | None = None,\n)\n</code></pre> <p>Pytest fixture facade: audioeval.stt / audioeval.tts / audioeval.samples.</p>"},{"location":"reference/pytest_audioeval/client/#pytest_audioeval.client.AudioEval.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Cleanup clients.</p>"},{"location":"reference/pytest_audioeval/plugin/","title":"plugin","text":""},{"location":"reference/pytest_audioeval/plugin/#pytest_audioeval.plugin","title":"plugin","text":"<p>pytest entry point \u2014 fixtures and hooks.</p>"},{"location":"reference/pytest_audioeval/plugin/#pytest_audioeval.plugin.pytest_addoption","title":"pytest_addoption","text":"<pre><code>pytest_addoption(parser: Any) -&gt; None\n</code></pre> <p>Register CLI options for audioeval.</p>"},{"location":"reference/pytest_audioeval/plugin/#pytest_audioeval.plugin.audioeval","title":"audioeval  <code>async</code>","text":"<pre><code>audioeval(\n    request: FixtureRequest,\n) -&gt; AsyncIterator[AudioEval]\n</code></pre> <p>Session-scoped AudioEval fixture with CLI-driven configuration.</p>"},{"location":"reference/pytest_audioeval/plugin/#pytest_audioeval.plugin.audioeval_thresholds","title":"audioeval_thresholds","text":"<pre><code>audioeval_thresholds(\n    request: FixtureRequest,\n) -&gt; dict[str, float]\n</code></pre> <p>Per-test threshold overrides from CLI.</p>"},{"location":"reference/pytest_audioeval/stt/","title":"stt","text":""},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt","title":"stt","text":"<p>STT evaluation client \u2014 httpx + httpx-ws + httpx-sse under the hood.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTResult","title":"STTResult  <code>dataclass</code>","text":"<pre><code>STTResult(\n    hypothesis_text: str = \"\",\n    text_metrics: TextMetrics | None = None,\n    latency_ms: float = 0.0,\n    chunks_received: int = 0,\n    fragments: list[str] = list(),\n)\n</code></pre> <p>STT evaluation result with optional metrics.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTResult.assert_quality","title":"assert_quality","text":"<pre><code>assert_quality(\n    *, max_wer: float = 0.2, max_cer: float = 0.15\n) -&gt; Self\n</code></pre> <p>Assert STT quality. Chainable.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTResult.compute_metrics","title":"compute_metrics","text":"<pre><code>compute_metrics(reference: str) -&gt; Self\n</code></pre> <p>Compute WER/CER against reference. Chainable.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTSession","title":"STTSession","text":"<pre><code>STTSession(\n    *,\n    session: AsyncWebSocketSession,\n    sample: AudioSample | None,\n)\n</code></pre> <p>Active WebSocket session for STT evaluation.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTSession.send_bytes","title":"send_bytes  <code>async</code>","text":"<pre><code>send_bytes(data: bytes) -&gt; None\n</code></pre> <p>Send binary audio data.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTSession.send_text","title":"send_text  <code>async</code>","text":"<pre><code>send_text(data: str) -&gt; None\n</code></pre> <p>Send text (JSON config, END_OF_AUDIO, etc.).</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTSession.send_sample","title":"send_sample  <code>async</code>","text":"<pre><code>send_sample(\n    sample: AudioSample, *, chunk_ms: int = 200\n) -&gt; None\n</code></pre> <p>Stream sample in chunks with realistic pacing.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTSession.receive_text","title":"receive_text  <code>async</code>","text":"<pre><code>receive_text(*, timeout: float | None = None) -&gt; str\n</code></pre> <p>Receive text frame and accumulate as fragment.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTSession.receive_bytes","title":"receive_bytes  <code>async</code>","text":"<pre><code>receive_bytes(*, timeout: float | None = None) -&gt; bytes\n</code></pre> <p>Receive binary frame.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTSession.result","title":"result","text":"<pre><code>result() -&gt; STTResult\n</code></pre> <p>Build STTResult from accumulated fragments.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTClient","title":"STTClient","text":"<pre><code>STTClient(*, url: str, timeout: float = 30.0)\n</code></pre> <p>STT evaluation client \u2014 HTTP batch + WebSocket streaming.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTClient.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    *, data: bytes | None = None, **kwargs: Any\n) -&gt; httpx.Response\n</code></pre> <p>Batch POST audio to STT endpoint (e.g. OpenAI Whisper API). Returns raw httpx.Response.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTClient.stream","title":"stream  <code>async</code>","text":"<pre><code>stream(\n    *, data: bytes | None = None, **kwargs: Any\n) -&gt; AsyncIterator[httpx.Response]\n</code></pre> <p>Chunked streaming POST. Yields httpx.Response for aiter_bytes/aiter_lines.</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTClient.sse","title":"sse  <code>async</code>","text":"<pre><code>sse(\n    *, data: bytes | None = None, **kwargs: Any\n) -&gt; AsyncIterator[EventSource]\n</code></pre> <p>SSE streaming POST. Yields EventSource for aiter_sse().</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTClient.ws","title":"ws  <code>async</code>","text":"<pre><code>ws(\n    *, sample: AudioSample | None = None, **kwargs: Any\n) -&gt; AsyncIterator[STTSession]\n</code></pre> <p>Open WebSocket session for STT streaming (e.g. WhisperLive).</p>"},{"location":"reference/pytest_audioeval/stt/#pytest_audioeval.stt.STTClient.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>No-op \u2014 clients are created per-call.</p>"},{"location":"reference/pytest_audioeval/tts/","title":"tts","text":""},{"location":"reference/pytest_audioeval/tts/#pytest_audioeval.tts","title":"tts","text":"<p>TTS evaluation client \u2014 httpx + httpx-sse + httpx-ws under the hood.</p>"},{"location":"reference/pytest_audioeval/tts/#pytest_audioeval.tts.TTSClient","title":"TTSClient","text":"<pre><code>TTSClient(*, url: str, timeout: float = 30.0)\n</code></pre> <p>TTS evaluation client \u2014 HTTP batch, streaming, and SSE.</p>"},{"location":"reference/pytest_audioeval/tts/#pytest_audioeval.tts.TTSClient.post","title":"post  <code>async</code>","text":"<pre><code>post(\n    *, json: dict[str, Any] | None = None, **kwargs: Any\n) -&gt; httpx.Response\n</code></pre> <p>Batch POST to TTS endpoint. Returns raw httpx.Response.</p>"},{"location":"reference/pytest_audioeval/tts/#pytest_audioeval.tts.TTSClient.stream","title":"stream  <code>async</code>","text":"<pre><code>stream(\n    *, json: dict[str, Any] | None = None, **kwargs: Any\n) -&gt; AsyncIterator[httpx.Response]\n</code></pre> <p>Chunked streaming POST. Yields httpx.Response for aiter_bytes/aiter_lines.</p>"},{"location":"reference/pytest_audioeval/tts/#pytest_audioeval.tts.TTSClient.sse","title":"sse  <code>async</code>","text":"<pre><code>sse(\n    *, json: dict[str, Any] | None = None, **kwargs: Any\n) -&gt; AsyncIterator[EventSource]\n</code></pre> <p>SSE streaming POST. Yields EventSource for aiter_sse().</p>"},{"location":"reference/pytest_audioeval/tts/#pytest_audioeval.tts.TTSClient.ws","title":"ws  <code>async</code>","text":"<pre><code>ws(**kwargs: Any) -&gt; AsyncIterator[AsyncWebSocketSession]\n</code></pre> <p>Open WebSocket session for TTS streaming (e.g. WebSocket-based TTS servers).</p>"},{"location":"reference/pytest_audioeval/tts/#pytest_audioeval.tts.TTSClient.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>No-op \u2014 clients are created per-call.</p>"},{"location":"reference/pytest_audioeval/metrics/audio/","title":"audio","text":""},{"location":"reference/pytest_audioeval/metrics/audio/#pytest_audioeval.metrics.audio","title":"audio","text":"<p>Perceptual audio quality via PESQ.</p>"},{"location":"reference/pytest_audioeval/metrics/audio/#pytest_audioeval.metrics.audio.AudioMetrics","title":"AudioMetrics  <code>dataclass</code>","text":"<pre><code>AudioMetrics(mos: float, sample_rate: int)\n</code></pre> <p>PESQ MOS (1-5 scale).</p>"},{"location":"reference/pytest_audioeval/metrics/audio/#pytest_audioeval.metrics.audio.AudioMetrics.compute","title":"compute  <code>classmethod</code>","text":"<pre><code>compute(\n    reference: ndarray,\n    hypothesis: ndarray,\n    *,\n    sample_rate: int = 16000,\n) -&gt; Self\n</code></pre> <p>PESQ wideband comparison.</p>"},{"location":"reference/pytest_audioeval/metrics/audio/#pytest_audioeval.metrics.audio.AudioMetrics.assert_quality","title":"assert_quality","text":"<pre><code>assert_quality(*, min_mos: float = 3.0) -&gt; None\n</code></pre> <p>Raise AssertionError if MOS below threshold.</p>"},{"location":"reference/pytest_audioeval/metrics/text/","title":"text","text":""},{"location":"reference/pytest_audioeval/metrics/text/#pytest_audioeval.metrics.text","title":"text","text":"<p>Word/character-level transcription quality metrics.</p>"},{"location":"reference/pytest_audioeval/metrics/text/#pytest_audioeval.metrics.text.TextMetrics","title":"TextMetrics  <code>dataclass</code>","text":"<pre><code>TextMetrics(\n    wer: float,\n    cer: float,\n    substitutions: int,\n    insertions: int,\n    deletions: int,\n)\n</code></pre> <p>WER/CER transcription quality. O(n) via jiwer.</p>"},{"location":"reference/pytest_audioeval/metrics/text/#pytest_audioeval.metrics.text.TextMetrics.compute","title":"compute  <code>classmethod</code>","text":"<pre><code>compute(reference: str, hypothesis: str) -&gt; Self\n</code></pre> <p>Compute all text metrics in a single pass.</p>"},{"location":"reference/pytest_audioeval/metrics/text/#pytest_audioeval.metrics.text.TextMetrics.assert_quality","title":"assert_quality","text":"<pre><code>assert_quality(\n    *, max_wer: float = 0.2, max_cer: float = 0.15\n) -&gt; None\n</code></pre> <p>Raise AssertionError with detailed breakdown on failure.</p>"},{"location":"reference/pytest_audioeval/samples/registry/","title":"registry","text":""},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry","title":"registry","text":"<p>Sample catalog and ground-truth registry.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.SampleLang","title":"SampleLang","text":"<p>               Bases: <code>StrEnum</code></p> <p>Supported sample languages.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.AudioSample","title":"AudioSample  <code>dataclass</code>","text":"<pre><code>AudioSample(\n    name: str,\n    lang: SampleLang,\n    reference_text: str,\n    audio_path: Path,\n    sample_rate: int = 16000,\n    duration_ms: int = 0,\n)\n</code></pre> <p>Ground-truth pair: audio + expected transcription.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.AudioSample.audio_bytes","title":"audio_bytes","text":"<pre><code>audio_bytes() -&gt; bytes\n</code></pre> <p>Raw file bytes.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.AudioSample.audio_numpy","title":"audio_numpy","text":"<pre><code>audio_numpy() -&gt; np.ndarray\n</code></pre> <p>Load as float32 numpy array.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.AudioSample.chunks","title":"chunks","text":"<pre><code>chunks(chunk_ms: int = 200) -&gt; list[bytes]\n</code></pre> <p>Split audio into float32 PCM chunks for streaming. O(n/chunk_size).</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.SampleRegistry","title":"SampleRegistry","text":"<pre><code>SampleRegistry()\n</code></pre> <p>Catalog of embedded audio fixtures. O(1) lookup by name.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.SampleRegistry.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str) -&gt; AudioSample\n</code></pre> <p>Attribute-style access: samples.en_hello_world.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.SampleRegistry.all","title":"all","text":"<pre><code>all() -&gt; list[AudioSample]\n</code></pre> <p>All registered samples.</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.SampleRegistry.by_lang","title":"by_lang","text":"<pre><code>by_lang(lang: SampleLang) -&gt; list[AudioSample]\n</code></pre> <p>Filter samples by language. O(n).</p>"},{"location":"reference/pytest_audioeval/samples/registry/#pytest_audioeval.samples.registry.SampleRegistry.register","title":"register","text":"<pre><code>register(sample: AudioSample) -&gt; None\n</code></pre> <p>Register custom project-specific samples.</p>"}]}